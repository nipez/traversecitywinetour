{
  "name": "traversecitywinetour",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev . --persist-to .wrangler/state",
    "test": "node scripts/test_leads.mjs",
    "db:migrate:local": "wrangler d1 migrations apply tcwinetour-leads --local",
    "db:migrate": "wrangler d1 migrations apply tcwinetour-leads --remote"
  },
  "devDependencies": {
    "wrangler": "^4.33.0"
  }
}
